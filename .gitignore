# Build CMake (directory di build alla radice)
/build/
/build*/
/cmake-build*/

# Cartella di release assemblata (exe + data/), rigenerabile. Vedi docs/ARCHITETTURA.md §7.
/dist/

# Artefatti di compilazione
*.o
*.obj
*.a
*.lib
*.dll
*.dylib
*.so
*.exe
*.pdb
*.ilk

# L'eseguibile della demo macOS (build manuale alla radice)
/onehand_mac

# Metadati macOS
.DS_Store

# Modello bigrammi (~20 MB). Il tool di generazione (tools/build_bigrams) e' stato
# rimosso dal repo; per rigenerarlo recuperalo dalla storia git. Vedi docs/.
/data/it.bigrams.bin
# Archivio/estratto bigrammi (fuori repo di norma)
*.word.bigrams
